{{!-- ── Notes ── --}}
    <div class="tab" data-tab="notes" data-group="primary">
      <div class="section-title">Character Notes</div>
      <textarea class="notes-area" name="system.basic.notes">{{actor.system.basic.notes}}</textarea>
    </div>